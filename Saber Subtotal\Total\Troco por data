SELECT FEC_SAIDAS.NUMERO, FEC_SAIDAS.DATA, FEC_SAIDAS.subtotal, FEC_SAIDAS.totalpagar, FEC_SAIDAS.TROCO FROM FEC_SAIDAS
LEFT JOIN TIPOVEND ON TIPOVEND.NUMERO = FEC_SAIDAS.NUMERO
WHERE (FEC_SAIDAS.DATA = '2026-05-28') AND (TIPOVEND.CODTIPO = 1);
